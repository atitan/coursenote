<% provide(:title, "修改密碼") %>
<div class="bs-docs-section jumbotron">
  <div class="row">
    <h1 id="forms" class="text-center m-title"><strong>修改密碼</strong></h1>
    <div class="col-lg-8 col-lg-offset-2">
      <div class="well bs-component">
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :patch, class: "form-horizontal" }) do |f| %>
          <%= devise_error_messages! %>
          <%= f.hidden_field :reset_password_token %>

           <div class="form-group">
            <%= f.label :password, "新密碼", class: 'col-lg-2 control-label' %>
              <div class="col-lg-10">
                <%= f.password_field :password, autofocus: true, autocomplete: "off", class: 'form-control' %>
              </div>
          </div>
          <div class="form-group">
            <%= f.label :password_confirmation, "確認新密碼", class: 'col-lg-2 control-label' %>
              <div class="col-lg-10">
                <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
              </div>
          </div>
          <div class="form-group">
            <div class="col-lg-10 col-lg-offset-2">
              <%= f.submit "送出", class: "btn btn-lg m-btn" %>
            </div>
          </div>
        <% end %>

        <div class="col-lg-offset-2">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>